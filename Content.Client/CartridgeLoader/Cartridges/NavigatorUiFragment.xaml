<cartridges:NavigatorUiFragment xmlns:cartridges="clr-namespace:Content.Client.CartridgeLoader.Cartridges"
                                xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                                xmlns="https://spacestation14.io" Margin="1 0 2 0"
                                Orientation="Vertical">
    <PanelContainer StyleClasses="BackgroundDark"></PanelContainer>
    <BoxContainer Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True">
        <controls:StripeBack Name="StationNameContainer">
            <PanelContainer>
                <Label Name="StationNameLabel" Align="Center" Text="{Loc 'navigator-cartridge-loading'}" StyleClasses="SubText"/>
            </PanelContainer>
        </controls:StripeBack>
        <PanelContainer Name="NavMapContainer" HorizontalExpand="True" VerticalExpand="True" Margin="4">
        </PanelContainer>
        <!-- //fire add -->
        <BoxContainer Orientation="Vertical" HorizontalExpand="True" Margin="4 0 4 4">
            <LineEdit Name="FilterBar" PlaceHolder="{Loc 'station-map-filter-placeholder'}" HorizontalExpand="True" Margin="0 0 0 4"/>
            <ScrollContainer HorizontalExpand="True" VerticalExpand="True" MinHeight="70">
                <BoxContainer Name="BeaconButtons" Orientation="Vertical" HorizontalExpand="True" />
            </ScrollContainer>
        </BoxContainer>
        <!-- //fire end -->
    </BoxContainer>
</cartridges:NavigatorUiFragment>
